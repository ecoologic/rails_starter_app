<ul>
  <li><%= avatar_for(@user, :big) %></li>
  <li><%= mail_to @user.email %></li>
</ul>

<%= link_to 'Back', users_path %>

<p>
  <%= link_to "Edit", edit_user_registration_path if can?(:manage, @user) %>
</p>
