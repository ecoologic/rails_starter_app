<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Email</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <th><%= avatar_for(user, :small) %></th>
        <td><%= mail_to user.email %></td>
        <td><%= link_to 'Show', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>
